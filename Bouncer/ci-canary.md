# CI canary (throwaway)

Temporary file to trigger the "Lint, Test, and Build" workflow (it only runs on
changes under `Bouncer/**`). This PR is opened to verify CI executes, then closed
without merging — this file should never land on `main`.
